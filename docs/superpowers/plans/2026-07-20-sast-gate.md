# Controlled SAST Gate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add pinned Semgrep profiles with local rules, sanitized reports and expiring exact baselines.

**Architecture:** Local YAML profiles define approved checks; a Python runner invokes Semgrep twice for JSON/SARIF and applies baseline policy; a reusable workflow retains evidence.

**Tech Stack:** Semgrep 1.170.0, Python, YAML rules, GitHub Actions.

## Global Constraints

- Never accept remote rule URLs or arbitrary profile strings.
- Never retain source snippets or environment values in evidence.
- Baselines require exact scope, owner, statement and expiry.

### Task 1: Define policy tests
- [x] Add failing tests for profile allowlisting, expired baselines, exact suppression and workflow security.

### Task 2: Implement SAST policy engine
- [x] Add baseline validation, stable fingerprints, finding sanitization and report hashing.

### Task 3: Add controlled rules and workflow
- [x] Add Python and TypeScript profiles.
- [x] Add pinned Semgrep installation and reusable workflow.

### Task 4: Validate and publish
- [x] Run focused unit, compile and YAML parsing checks.
- [ ] Run Semgrep against rule fixtures and the complete repository in GitHub CI.
- [ ] Open a draft PR closing #25.
